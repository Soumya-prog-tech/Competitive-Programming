//  A. Minimize Arrangement

#include<bits/stdc++.h>

using namespace std;

#define DEBUG
#define ll long long

void solve() {
    int n, m;
    cin>>n>>m;
    vector<int> c(n);
    for (int i = 0; i<n; i++) {
        cin>>c[i];
    }
    
}

int main() {
    int t = 1;
    // cin>>t;
    while(t--) {
        solve();
    }
    return 0;
}
